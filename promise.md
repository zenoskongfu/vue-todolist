## Promise

Promise是一个承诺，当状态确定时，承诺会做一些事情。


承诺在未来的某个时间点会完成某个操作，并且可以提供一个回调函数来处理操作的结果。





Promise 是 JavaScript 中用于处理异步操作的对象。它代表了一个尚未完成但最终会完成的操作，并且可以提供一个回调函数来处理操作的结果。

Promise 有三种状态：

- Pending（进行中）：初始状态，待确认的状态
- Fulfilled（已成功）：表示操作已经成功完成。当resolve函数被调用的时候
- Rejected（已失败）：表示操作失败。当rejected函数被调用的时候


## promise 承诺的动作，是异步的

## promise 可以被反复地承诺动作(无论成功的动作，还是失败的动作)

一个promise'可以承诺无数件事

怎么进行承诺？通过then

## promise可以以链式的方式进行承诺

## 当promise被链式调用的时候，上一个动作的返回值会以promise的形式传给下一个then函数，如果没传，就相当于放回undefined

## then中的回调函数中，不仅可以返回一个普通值，也可以返回一个promise对象。然后该then函数返回的新promise对象的承诺的值，取决于回调函数中返回的promise对象的承诺的值

不仅值是一致的，状态也是一致的

如果返回的是普通值，那么then函数返回的promise对象的状态就是成功的



## promise失败的状态，在链式调用中，是没有传染性的





